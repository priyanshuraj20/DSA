#include<bits/stdc++.h>
using namespace std;
//last in first out :
int main(){
    stack<int> s;
    s.push(10);
    s.push(10) ;

    s.pop();
    s.size();

    s.empty();

    s.top(); //stack ke top mein jo para hoga woh milega

    //swap : s1.swap(s2);

    //stack mein iterator nahi hote;
    //pop karke iterate karna hoga

     return 0;
}